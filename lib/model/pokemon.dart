class Pokemon {
  int number;
  String name;
  int height;
  int weight;
  String mainImageUrl;
  String demoImageUrl;
  List<String> types;
  String flavorText;

  Pokemon(this.number, this.name, this.height, this.weight, this.mainImageUrl,
      this.demoImageUrl, this.types, this.flavorText);
}
